<resources>

    <!-- Base application theme.-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here.-->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="BankImages">
        <item name="android:layout_height">@dimen/height</item>
        <item name="android:layout_width">@dimen/width</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
    </style>
    <style name="BankPages">
        <item name="android:layout_height">@dimen/main</item>
        <item name="android:layout_width">@dimen/main</item>
        <item name="android:layout_marginStart">@dimen/margin</item>
    </style>
</resources>
